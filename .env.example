# 这是 Smooth Gateway 的配置文件模板。
# 使用前，请将此文件复制为 .env，并填入您自己的配置。
# --- 必需配置 ---
# 您的上游 API 地址
UPSTREAM_API_URL=https://your-api-provider.com/v1/chat/completions

# --- 可选配置 (不填则使用默认值) ---
# 网关监听的端口 (默认: 3001)
# PORT=3001

# 日志级别 (debug 或 silent) (默认: silent)
# LOG_LEVEL=debug

# --- 可选的流式优化参数 ---
# STREAM_MIN_DELAY=0.016
# STREAM_MAX_DELAY=0.024
# STREAM_SHORT_TEXT_THRESHOLD=10
# STREAM_LONG_TEXT_THRESHOLD=50
# STREAM_CHUNK_SIZE=5
